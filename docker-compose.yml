services:

  db:
    image: postgres:16
    container_name: case_mgmt_db
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "5432:5432"        # exposes Postgres locally so you can connect via pgAdmin or psql
    volumes:
      - postgres_data:/var/lib/postgresql/data   # persists data between container restarts
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5

  app:
    build: .
    container_name: case_mgmt_app
    restart: always
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
    depends_on:
      db:
        condition: service_healthy   # waits for Postgres to be ready before starting the app
    volumes:
      - .:/app                       # mounts your local code into the container for hot reload

volumes:
  postgres_data:
